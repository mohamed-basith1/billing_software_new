import { createTheme } from "@mui/material/styles";

// Create a theme with Poppins
const theme = createTheme({
  typography: {
    fontFamily: `"Poppins", "Helvetica", "Arial", sans-serif`,
  },
});

export default theme;
